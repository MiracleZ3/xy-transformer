# === 设计文档与论文（本地保留，不进仓） ===
docs/
PANTHER*.pdf
*.pdf

# === 原始数据集（本地保留，不进仓） ===
# data_source/ 全部原始数据（全量 + 切片）均不进仓，避免仓库膨胀。
# 需要时按 README 自行下载到 data_source/，或重跑 simulate_*.py 生成本地副本。
data_source/

# === 数据工程中间产物（脚本可重新生成，不进仓） ===
data_processed/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/

# === 虚拟环境 ===
.venv/
venv/
env/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
Thumbs.db

# === Jupyter ===
.ipynb_checkpoints/

# === Test / coverage ===
.pytest_cache/
.coverage
htmlcov/

# === 训练输出（脚本可重新生成，不进仓） ===
model_out/
